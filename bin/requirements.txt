# DevOps Python依赖包
#
# Python版本要求:
#   - Python 3.6+: 支持基本功能
#   - Python 3.7+: 推荐版本，支持所有功能
#
# 安装方法:
#   pip3 install -r requirements.txt
#   或者
#   pip3 install --user -r requirements.txt
#
# 如果遇到版本冲突，请根据Python版本选择:
#   Python 3.6: pip3 install "PyYAML>=5.1,<7.0" "Jinja2>=2.10,<3.1"
#   Python 3.7+: pip3 install "PyYAML>=5.4.1" "Jinja2>=2.11.0"

# YAML解析库 (兼容Python 3.6+)
PyYAML>=5.1

# 模板引擎库 (兼容Python 3.6+)
Jinja2>=2.10
